<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Rdpeek.Agent</RootNamespace>
    <AssemblyName>rdpeek-agent</AssemblyName>
    <Platforms>x64</Platforms>
    <PlatformTarget>x64</PlatformTarget>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Rdpeek.Protocol\Rdpeek.Protocol.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- ETW fallback source for per-DVC traffic (see DvcTrafficWatcher). -->
    <PackageReference Include="Microsoft.Diagnostics.Tracing.TraceEvent" Version="3.1.19" />
    <!-- WMI (Win32_QuickFixEngineering / VideoController / PnPEntity) for deep system detail. -->
    <PackageReference Include="System.Management" Version="8.0.0" />
  </ItemGroup>

</Project>
